{
  "name": "@ospex/cli",
  "version": "0.4.4",
  "description": "Command-line interface for the Ospex protocol.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ospex": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "yarn workspace @ospex/sdk build && rimraf dist && node scripts/bundle.mjs",
    "clean": "rimraf dist",
    "dev": "tsx src/index.ts",
    "test": "yarn workspace @ospex/sdk build && vitest run",
    "test:watch": "vitest",
    "typecheck": "yarn workspace @ospex/sdk build && tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@commander-js/extra-typings": "^14.0.0",
    "@types/node": "^20.17.32",
    "cli-table3": "^0.6.5",
    "commander": "^14.0.0",
    "esbuild": "^0.28.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "viem": "^2.21.0",
    "vitest": "^2.1.8",
    "zod": "^3.23.8"
  }
}
